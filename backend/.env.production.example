# ===========================================
# PRODUÇÃO - dados reais, cliente final
# ===========================================
# Configure no painel do Render/Fly/Vercel.
# Nunca commitar .env.production com valores reais.

APP_ENV=production
NODE_ENV=production

DATABASE_URL="postgresql://..."
JWT_SECRET="..."
JWT_EXPIRES_IN=7d

PORT=3001
FRONTEND_URL="https://app.seudominio.com"
