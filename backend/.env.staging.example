# ===========================================
# HOMOLOGAÇÃO - banco e recursos próprios
# ===========================================
# Nunca use DATABASE_URL de produção aqui.

APP_ENV=staging
NODE_ENV=production

DATABASE_URL="postgresql://user:password@host-staging:5432/aurix_staging?schema=public"
JWT_SECRET="staging-secret-different-from-production"
JWT_EXPIRES_IN=7d

PORT=3001
FRONTEND_URL="https://staging.seudominio.com"
